generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./mental_health.sqlite"
}

model Answer {
  id Int @id @default(autoincrement())
  AnswerText String?
  SurveyID   Int?
  UserID     Int?
  QuestionID Int?
}